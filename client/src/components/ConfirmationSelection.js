import React from 'react';
import { View, Text } from 'react-native';

const ConfirmationSelection = () => {
  return (
    <View>
      <Text> Confirm button </Text>
    </View>
  )
};

export default ConfirmationSelection;